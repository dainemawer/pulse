/* global React, ReactDOM */
import { Widget } from '@pulse/ui';

ReactDOM.createRoot(document.getElementById('pulse-dashboard-widget')).render(
	<React.StrictMode>
		<Widget />
	</React.StrictMode>,
);
